/// Exposes API key middleware utilities for control-plane route protection.
pub mod auth_middleware;

/// Exposes server-specific HTTP adapters for integration testing and composition.
pub mod webhook_adapter;
